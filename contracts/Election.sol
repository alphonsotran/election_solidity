pragma solidity ^0.4.2;

contract Election {
    // model a candidate
    struct Candidate {
        uint id;
        string name;
        uint votecount;
    }
    // store candidates
    // fetch candidate
    // store candidates count

    function Election () public {
      
    }
}